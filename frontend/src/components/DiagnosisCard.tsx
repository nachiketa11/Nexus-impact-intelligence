export function DiagnosisCard({ data }: { data?: Record<string, unknown> }) { return <section className="panel p-5"><p className="eyebrow">Agent output</p><h2 className="mt-2 font-semibold text-white">Diagnosis</h2><p className="mt-4 text-sm leading-6 text-muted">{data ? String(data.root_cause ?? 'No root cause returned.') : 'Run an investigation to generate a diagnosis.'}</p>{data?.confidence !== undefined && <p className="mt-3 text-xs text-samsung">Confidence: {String(data.confidence)}</p>}</section> }
