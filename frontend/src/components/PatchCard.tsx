export function PatchCard({ data }: { data?: Record<string, unknown> }) { return <section className="panel p-5"><p className="eyebrow">Patch stage</p><h2 className="mt-2 font-semibold text-white">Suggested patch</h2><div className="mt-4 max-h-36 overflow-auto rounded-xl bg-ink/70 p-4 font-mono text-xs text-muted">{data ? String(data.diff ?? data.description ?? JSON.stringify(data)) : 'Run an investigation to generate a review-ready patch.'}</div></section> }
