export function EvidenceTrail({ data }: { data?: Record<string, unknown>[] }) { return <section className="panel p-5"><h2 className="font-semibold text-white">Evidence trail</h2>{data?.length ? <div className="mt-5 max-h-40 space-y-2 overflow-auto">{data.map((item, index) => <div className="rounded-xl bg-ink/60 p-3 text-xs text-muted" key={index}>{String(item.summary ?? item.description ?? JSON.stringify(item))}</div>)}</div> : <div className="mt-5 rounded-2xl border border-dashed border-white/10 p-6 text-center text-sm text-muted">Run an investigation to collect evidence.</div>}</section> }
